spring.datasource.url=${DB_URL:jdbc:postgresql://localhost:5432/nidus-test}
spring.datasource.username=${DB_USER:nidus}
spring.datasource.password=${DB_PASSWORD:nidus}
spring.jpa.hibernate.ddl-auto=update
ml-service.url=${ML_SERVICE_URL:http://localhost:8081}
ml-service.timeout=${ML_SERVICE_TIMEOUT:5000}
